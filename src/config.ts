export const config = {
  worldWidth: 10240,
  worldHeight: 10240,

  maxBirbs: 10000,
  birbSpeed: 4,
  turnSpeed: 0.05,

  visualDistance: 128,
  minDistance: 64,

  alignmentFactor: 0.5,
  cohesionFactor: 0.1,
  separationFactor: 0.2,
};

export const camera = {
  scaleAmount: 1.1,
  minScale: 0.1,
  maxScale: 1,
};
